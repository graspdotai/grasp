{
  "name": "grasp",
  "version": "0.1.0",
  "private": true,
  "description": "Grasp — voice-first AI learning platform (frontend)",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "lint": "npm run lint -w web"
  },
  "engines": {
    "node": ">=20"
  }
}
